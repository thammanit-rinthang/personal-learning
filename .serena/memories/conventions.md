Server Actions/route handlers/MCP adapters are thin and must use services, never Prisma. Service mutations: validate Zod, authorize actor, audit, transaction for multi-record changes. All published content must create revisions rather than overwrite. MCP permissions are scoped and all MCP writes draft-only; publish scope prohibited in MVP. Use Module term in domain code. No comments unless asked.